# El job runner lanza scripts/run_experimental.py y collect_metrics/collect_geo_live.py
# con el python del venv del backend (sys.executable), así que necesita las mismas
# dependencias que el pipeline de investigación.
-r ../requirements.txt

fastapi>=0.111,<1
uvicorn[standard]>=0.29,<1
pydantic>=2,<3
pydantic-settings>=2,<3
httpx>=0.27,<1
pytest>=8,<9
pytest-asyncio>=0.23,<1
pytest-cov>=5,<6
anyio>=4,<5
